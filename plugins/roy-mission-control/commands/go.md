---
description: Accept the result already shown and run the proposed next action when its checks pass.
---

Load `captain`. Follow its `go` section. An active interview, running agent, unresolved
blocker, or result not yet shown needs the captain's guidance before proceeding.

For user questions, prefer `AskUserQuestion`.
Load `agent-handoff` and follow its shared user-question policy before asking.
