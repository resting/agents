---
description: Accept the result shown at the last pause and resume the mission.
---

Load `captain`. Follow its `go` section. If nothing is waiting on the user, print the
progress block and say what is running or what comes next instead. An active
interview, running agent, or unresolved blocker needs the captain's guidance before
proceeding.
