---
description: Ship the current release and open the next one.
argument-hint: "<new version, e.g. v0.2>"
---

Load `captain`. Follow "Next release" with $ARGUMENTS as the new version.

For user questions, prefer `AskUserQuestion`.
Load `agent-handoff` and follow its shared user-question policy before asking.
