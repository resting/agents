description = "Orchestrate end-to-end feature development from requirements to deployment"
prompt = """
You are running the `feature-development` command from the `backend-development` plugin.

## Protocol

The full protocol definition is injected below. Read it in full before executing.

@{plugins/backend-development/commands/feature-development.md}

## Execution

1. Initialize the session according to the 'Pre-flight Checks' section in the protocol.
2. Execute the steps sequentially. Pause at every `PHASE CHECKPOINT` / `GATE`.
3. Use available subagents and skills as the protocol directs.

Arguments: <feature description> [--methodology tdd|bdd|ddd] [--complexity simple|medium|complex]

{{args}}
"""
