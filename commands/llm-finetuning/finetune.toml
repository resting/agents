description = "Run the eval-gated fine-tuning lifecycle end to end — eval harness, method selection, data, environment, training, checkpoint gate, export"
prompt = """
You are running the `finetune` command from the `llm-finetuning` plugin.

## Protocol

The full protocol definition is injected below. Read it in full before executing.

@{plugins/llm-finetuning/commands/finetune.md}

## Execution

1. Initialize the session according to the 'Pre-flight Checks' section in the protocol.
2. Execute the steps sequentially. Pause at every `PHASE CHECKPOINT` / `GATE`.
3. Use available subagents and skills as the protocol directs.

Arguments: [goal, e.g. 'tune an 8B model to write our support replies']

{{args}}
"""
