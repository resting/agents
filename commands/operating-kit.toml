description = "Portable operating method: session lifecycle agents (session-start/end), pre-ship code review, deploy with live verification, and production log health check. All agents use {{placeholders}} that adapt to your project."
prompt = """
Portable operating method: session lifecycle agents (session-start/end), pre-ship code review, deploy with live verification, and production log health check. All agents use {{placeholders}} that adapt to your project.

This is the entry point for the `operating-kit` plugin.

Subagents: `operating-kit__code-review-preshipment`, `operating-kit__deploy-with-verification`, `operating-kit__prod-logs-health-check`, `operating-kit__session-end`, `operating-kit__session-start`. Invoke with `@<agent>` syntax.

{{args}}
"""
