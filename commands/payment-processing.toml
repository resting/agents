description = "Payment gateway integration with Stripe, PayPal, checkout flow implementation, subscription billing, and PCI compliance"
prompt = """
Payment gateway integration with Stripe, PayPal, checkout flow implementation, subscription billing, and PCI compliance.

This is the entry point for the `payment-processing` plugin.

Subagents: `payment-processing__payment-integration`. Invoke with `@<agent>` syntax.

Skills: `payment-processing__billing-automation`, `payment-processing__paypal-integration`, `payment-processing__pci-compliance`, `payment-processing__stripe-integration`. Describe a matching task to activate.

{{args}}
"""
