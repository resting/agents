description = "Spawn an agent team using presets (review, debug, feature, fullstack, research, security, migration) or custom composition"
prompt = """
You are running the `team-spawn` command from the `agent-teams` plugin.

## Protocol

The full protocol definition is injected below. Read it in full before executing.

@{plugins/agent-teams/commands/team-spawn.md}

## Execution

1. Initialize the session according to the 'Pre-flight Checks' section in the protocol.
2. Execute the steps sequentially. Pause at every `PHASE CHECKPOINT` / `GATE`.
3. Use available subagents and skills as the protocol directs.

Arguments: <preset|custom> [--name team-name] [--members N] [--delegate]

{{args}}
"""
