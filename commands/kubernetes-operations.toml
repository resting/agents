description = "Kubernetes manifest generation, networking configuration, security policies, observability setup, GitOps workflows, and auto-scaling"
prompt = """
Kubernetes manifest generation, networking configuration, security policies, observability setup, GitOps workflows, and auto-scaling.

This is the entry point for the `kubernetes-operations` plugin.

Subagents: `kubernetes-operations__kubernetes-architect`. Invoke with `@<agent>` syntax.

Skills: `kubernetes-operations__gitops-workflow`, `kubernetes-operations__helm-chart-scaffolding`, `kubernetes-operations__k8s-manifest-generator`, `kubernetes-operations__k8s-security-policies`. Describe a matching task to activate.

{{args}}
"""
