description = "Dependency auditing, version management, and security vulnerability scanning"
prompt = """
Dependency auditing, version management, and security vulnerability scanning.

This is the entry point for the `dependency-management` plugin.

Subagents: `dependency-management__legacy-modernizer`. Invoke with `@<agent>` syntax.

Commands: `/dependency-management:deps-audit`.

{{args}}
"""
