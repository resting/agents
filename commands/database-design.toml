description = "Database architecture, schema design, and SQL optimization for production systems"
prompt = """
Database architecture, schema design, and SQL optimization for production systems.

This is the entry point for the `database-design` plugin.

Subagents: `database-design__database-architect`, `database-design__sql-pro`. Invoke with `@<agent>` syntax.

Skills: `database-design__postgresql`. Describe a matching task to activate.

{{args}}
"""
