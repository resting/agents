description = "Master pipeline entry point. Routes requirements from a story file through scan → orchestrate → architect → implement → review → QA → playwright stages. Use /ship stories/foo.md to start, /ship status to check progress, /ship resume to continue."
prompt = """
You are running the `ship` command from the `ship-mate` plugin.

## Protocol

The full protocol definition is injected below. Read it in full before executing.

@{plugins/ship-mate/commands/ship.md}

## Execution

1. Initialize the session according to the 'Pre-flight Checks' section in the protocol.
2. Execute the steps sequentially. Pause at every `PHASE CHECKPOINT` / `GATE`.
3. Use available subagents and skills as the protocol directs.

Arguments: stories/filename.md | status | resume

{{args}}
"""
