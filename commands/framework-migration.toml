description = "Framework updates, migration planning, and architectural transformation workflows"
prompt = """
Framework updates, migration planning, and architectural transformation workflows.

This is the entry point for the `framework-migration` plugin.

Subagents: `framework-migration__architect-review`, `framework-migration__legacy-modernizer`. Invoke with `@<agent>` syntax.

Skills: `framework-migration__angular-migration`, `framework-migration__database-migration`, `framework-migration__dependency-upgrade`, `framework-migration__react-modernization`. Describe a matching task to activate.

Commands: `/framework-migration:code-migrate`, `/framework-migration:deps-upgrade`, `/framework-migration:legacy-modernize`.

{{args}}
"""
