description = "WCAG accessibility auditing, compliance validation, UI testing for screen readers, keyboard navigation, and inclusive design"
prompt = """
WCAG accessibility auditing, compliance validation, UI testing for screen readers, keyboard navigation, and inclusive design.

This is the entry point for the `accessibility-compliance` plugin.

Subagents: `accessibility-compliance__ui-visual-validator`. Invoke with `@<agent>` syntax.

Skills: `accessibility-compliance__screen-reader-testing`, `accessibility-compliance__wcag-audit-patterns`. Describe a matching task to activate.

Commands: `/accessibility-compliance:accessibility-audit`.

{{args}}
"""
