description = "Orchestrate legacy system modernization using the strangler fig pattern with gradual component replacement"
prompt = """
You are running the `legacy-modernize` command from the `framework-migration` plugin.

## Protocol

The full protocol definition is injected below. Read it in full before executing.

@{plugins/framework-migration/commands/legacy-modernize.md}

## Execution

1. Initialize the session according to the 'Pre-flight Checks' section in the protocol.
2. Execute the steps sequentially. Pause at every `PHASE CHECKPOINT` / `GATE`.
3. Use available subagents and skills as the protocol directs.

Arguments: <legacy codebase path or description> [--strategy parallel-systems|big-bang|by-feature|database-first|api-first]

{{args}}
"""
