description = "Distributed system tracing and debugging across microservices"
prompt = """
Distributed system tracing and debugging across microservices.

This is the entry point for the `distributed-debugging` plugin.

Subagents: `distributed-debugging__devops-troubleshooter`, `distributed-debugging__error-detective`. Invoke with `@<agent>` syntax.

Commands: `/distributed-debugging:debug-trace`.

{{args}}
"""
