description = "Error analysis, trace debugging, and multi-agent problem diagnosis"
prompt = """
Error analysis, trace debugging, and multi-agent problem diagnosis.

This is the entry point for the `error-debugging` plugin.

Subagents: `error-debugging__debugger`, `error-debugging__error-detective`. Invoke with `@<agent>` syntax.

Commands: `/error-debugging:error-analysis`, `/error-debugging:error-trace`, `/error-debugging:multi-agent-review`.

{{args}}
"""
