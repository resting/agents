description = "Analyze and resolve errors across the full application lifecycle — from stack traces to distributed tracing — using systematic root-cause analysis and observability tools."
prompt = """
You are running the `error-analysis` command from the `error-diagnostics` plugin.

## Protocol

The full protocol definition is injected below. Read it in full before executing.

@{plugins/error-diagnostics/commands/error-analysis.md}

## Execution

1. Initialize the session according to the 'Pre-flight Checks' section in the protocol.
2. Execute the steps sequentially. Pause at every `PHASE CHECKPOINT` / `GATE`.
3. Use available subagents and skills as the protocol directs.

{{args}}
"""
