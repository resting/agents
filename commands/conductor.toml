description = "Context-Driven Development plugin that transforms Claude Code into a project management tool with structured workflow: Context → Spec & Plan → Implement"
prompt = """
Context-Driven Development plugin that transforms Claude Code into a project management tool with structured workflow: Context → Spec & Plan → Implement.

This is the entry point for the `conductor` plugin.

Subagents: `conductor__conductor-validator`. Invoke with `@<agent>` syntax.

Skills: `conductor__context-driven-development`, `conductor__track-management`, `conductor__workflow-patterns`. Describe a matching task to activate.

Commands: `/conductor:implement`, `/conductor:manage`, `/conductor:new-track`, `/conductor:revert`, `/conductor:setup`, `/conductor:status`.

{{args}}
"""
