description = "Build features guided by data insights, A/B testing, and continuous measurement"
prompt = """
You are running the `data-driven-feature` command from the `data-engineering` plugin.

## Protocol

The full protocol definition is injected below. Read it in full before executing.

@{plugins/data-engineering/commands/data-driven-feature.md}

## Execution

1. Initialize the session according to the 'Pre-flight Checks' section in the protocol.
2. Execute the steps sequentially. Pause at every `PHASE CHECKPOINT` / `GATE`.
3. Use available subagents and skills as the protocol directs.

Arguments: <feature description> [--experiment-type ab|multivariate|bandit] [--confidence 0.90|0.95|0.99]

{{args}}
"""
