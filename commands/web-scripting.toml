description = "Web scripting with PHP and Ruby for web applications, CMS development, and backend services"
prompt = """
Web scripting with PHP and Ruby for web applications, CMS development, and backend services.

This is the entry point for the `web-scripting` plugin.

Subagents: `web-scripting__php-pro`, `web-scripting__ruby-pro`. Invoke with `@<agent>` syntax.

{{args}}
"""
