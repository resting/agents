description = "Orchestrate end-to-end full-stack feature development across backend, frontend, database, and infrastructure layers"
prompt = """
You are running the `full-stack-feature` command from the `full-stack-orchestration` plugin.

## Protocol

The full protocol definition is injected below. Read it in full before executing.

@{plugins/full-stack-orchestration/commands/full-stack-feature.md}

## Execution

1. Initialize the session according to the 'Pre-flight Checks' section in the protocol.
2. Execute the steps sequentially. Pause at every `PHASE CHECKPOINT` / `GATE`.
3. Use available subagents and skills as the protocol directs.

Arguments: <feature description> [--stack react/fastapi/postgres] [--api-style rest|graphql] [--complexity simple|medium|complex]

{{args}}
"""
