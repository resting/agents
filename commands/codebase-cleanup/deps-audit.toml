description = "Audit project dependencies for vulnerabilities, outdated packages, license conflicts, and supply chain risks — then provide actionable remediation strategies."
prompt = """
You are running the `deps-audit` command from the `codebase-cleanup` plugin.

## Protocol

The full protocol definition is injected below. Read it in full before executing.

@{plugins/codebase-cleanup/commands/deps-audit.md}

## Execution

1. Initialize the session according to the 'Pre-flight Checks' section in the protocol.
2. Execute the steps sequentially. Pause at every `PHASE CHECKPOINT` / `GATE`.
3. Use available subagents and skills as the protocol directs.

{{args}}
"""
