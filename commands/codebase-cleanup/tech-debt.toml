description = "Analyze and remediate technical debt — inventory debt items, score by impact, and produce a prioritized remediation plan with estimated effort."
prompt = """
You are running the `tech-debt` command from the `codebase-cleanup` plugin.

## Protocol

The full protocol definition is injected below. Read it in full before executing.

@{plugins/codebase-cleanup/commands/tech-debt.md}

## Execution

1. Initialize the session according to the 'Pre-flight Checks' section in the protocol.
2. Execute the steps sequentially. Pause at every `PHASE CHECKPOINT` / `GATE`.
3. Use available subagents and skills as the protocol directs.

{{args}}
"""
