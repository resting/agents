description = "Execute a comprehensive TDD workflow with strict red-green-refactor discipline"
prompt = """
You are running the `tdd-cycle` command from the `tdd-workflows` plugin.

## Protocol

The full protocol definition is injected below. Read it in full before executing.

@{plugins/tdd-workflows/commands/tdd-cycle.md}

## Execution

1. Initialize the session according to the 'Pre-flight Checks' section in the protocol.
2. Execute the steps sequentially. Pause at every `PHASE CHECKPOINT` / `GATE`.
3. Use available subagents and skills as the protocol directs.

Arguments: <feature or module to implement> [--incremental|--suite] [--coverage 80]

{{args}}
"""
