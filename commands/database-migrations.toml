description = "Database migration automation, observability, and cross-database migration strategies"
prompt = """
Database migration automation, observability, and cross-database migration strategies.

This is the entry point for the `database-migrations` plugin.

Subagents: `database-migrations__database-admin`, `database-migrations__database-optimizer`. Invoke with `@<agent>` syntax.

Commands: `/database-migrations:migration-observability`, `/database-migrations:sql-migrations`.

{{args}}
"""
