description = "Orchestrate git workflow from code review through PR creation with quality gates"
prompt = """
You are running the `git-workflow` command from the `git-pr-workflows` plugin.

## Protocol

The full protocol definition is injected below. Read it in full before executing.

@{plugins/git-pr-workflows/commands/git-workflow.md}

## Execution

1. Initialize the session according to the 'Pre-flight Checks' section in the protocol.
2. Execute the steps sequentially. Pause at every `PHASE CHECKPOINT` / `GATE`.
3. Use available subagents and skills as the protocol directs.

Arguments: <target branch> [--skip-tests] [--draft-pr] [--no-push] [--squash] [--conventional] [--trunk-based]

{{args}}
"""
