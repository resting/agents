description = "Git workflow automation, pull request enhancement, and team onboarding processes"
prompt = """
Git workflow automation, pull request enhancement, and team onboarding processes.

This is the entry point for the `git-pr-workflows` plugin.

Subagents: `git-pr-workflows__code-reviewer`. Invoke with `@<agent>` syntax.

Commands: `/git-pr-workflows:git-workflow`, `/git-pr-workflows:onboard`, `/git-pr-workflows:pr-enhance`.

{{args}}
"""
