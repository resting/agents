description = "Orchestrate multi-agent incident response with modern SRE practices for rapid resolution and learning"
prompt = """
You are running the `incident-response` command from the `incident-response` plugin.

## Protocol

The full protocol definition is injected below. Read it in full before executing.

@{plugins/incident-response/commands/incident-response.md}

## Execution

1. Initialize the session according to the 'Pre-flight Checks' section in the protocol.
2. Execute the steps sequentially. Pause at every `PHASE CHECKPOINT` / `GATE`.
3. Use available subagents and skills as the protocol directs.

Arguments: <incident description> [--severity P0|P1|P2|P3]

{{args}}
"""
