description = "Pre-deployment checks, configuration validation, and deployment readiness assessment"
prompt = """
Pre-deployment checks, configuration validation, and deployment readiness assessment.

This is the entry point for the `deployment-validation` plugin.

Subagents: `deployment-validation__cloud-architect`. Invoke with `@<agent>` syntax.

Commands: `/deployment-validation:config-validate`.

{{args}}
"""
