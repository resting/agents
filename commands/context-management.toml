description = "Context persistence, restoration, and long-running conversation management"
prompt = """
Context persistence, restoration, and long-running conversation management.

This is the entry point for the `context-management` plugin.

Subagents: `context-management__context-manager`. Invoke with `@<agent>` syntax.

Commands: `/context-management:context-restore`, `/context-management:context-save`.

{{args}}
"""
