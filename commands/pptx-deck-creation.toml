description = "Create production-ready, editable PowerPoint decks through a spec-first, coordinate-explicit workflow with read-only reference analysis and quality gates."
prompt = """
Create production-ready, editable PowerPoint decks through a spec-first, coordinate-explicit workflow with read-only reference analysis and quality gates.

This is the entry point for the `pptx-deck-creation` plugin.

Subagents: `pptx-deck-creation__pptx-deck-creation-builder`. Invoke with `@<agent>` syntax.

Skills: `pptx-deck-creation__pptx-deck-context`, `pptx-deck-creation__pptx-quality-gates`, `pptx-deck-creation__pptx-reference-deck-analysis`, `pptx-deck-creation__pptx-slide-specification`, `pptx-deck-creation__pptx-visual-assets`. Describe a matching task to activate.

{{args}}
"""
