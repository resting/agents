description = "Three-layer quality evaluation framework for Claude Code plugins with Elo ranking"
prompt = """
Three-layer quality evaluation framework for Claude Code plugins with Elo ranking.

This is the entry point for the `plugin-eval` plugin.

Subagents: `plugin-eval__eval-judge`, `plugin-eval__eval-orchestrator`. Invoke with `@<agent>` syntax.

Skills: `plugin-eval__evaluation-methodology`. Describe a matching task to activate.

Commands: `/plugin-eval:certify`, `/plugin-eval:compare`, `/plugin-eval:eval`.

{{args}}
"""
